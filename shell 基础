一.shell是什么
  Shell是一个命令行解释器，它为用户提供了一个向Linux内核发送请求以便运行程序界面系统级程序，用户可以用
shell来启动，挂起，停止甚至是编写一些程序。

接收命令，翻译成0101，充当内核和用户的沟通桥梁

shell可以嵌套调用

二.脚本执行方式
1.echo 选项  输出内容

-e  ASCII码也能输出
\a 
\b 退格键
\n 换行键
\r 
\t tab
\v

开启颜色和关闭颜色 一起使用  \e[1;31m     \e[0m  

2.
写shell文件 开头一定要写 #！/bin/bash   
#后边都是注释

3.shell脚本的执行
赋予执行权限，直接运行
chmod 755 hello.sh
./hello.sh
通过Bash调用执行脚本
bash hello.sh

三.Bash的基本功能
